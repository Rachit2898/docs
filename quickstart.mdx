---
title: "Quickstart"
description: "Start using the Upon AI Admin API in minutes"
---

## ⚡ Upon AI Admin API - Quickstart

This documentation helps you start using the Upon AI Admin API right away. We provide simple, secure HTTP endpoints that can be used directly — no SDK or library required.

---

### 🔐 Authentication

All endpoints are secured using a Bearer token. You will receive your API key during onboarding.

Include the following header in **every** request.

### ✅ Example: Get All Users

```bash
# Request
curl -X GET https://api.uponai.com/v1/users \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json"

# Response
[
  {
    "id": "user_123",
    "name": "John Doe",
    "email": "john@example.com"
  },
  {
    "id": "user_456",
    "name": "Jane Smith",
    "email": "jane@example.com"
  }
]
```